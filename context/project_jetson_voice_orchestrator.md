---
name: jetson-voice-orchestrator
description: C++ voice pipeline daemon for the Jetson avatar (hotword/STT/LLM/TTS + auto model offload-onload)
metadata: 
  node_type: memory
  type: project
  originSessionId: c340df7a-1285-4391-94c3-c966be977f05
---

**2026-06-13: building the avatar's voice pipeline as a LIGHT C++ daemon** (user wants it in C++ to save RAM/GPU, with automatic model offload/onload). Pairs with the PyQt UI [[jetson-qt-kiosk]] and the [[avatar-studio-api]]. Box state: [[jetson-wiped-2026-06-12]] (reach via Bikram Wi-Fi 192.168.0.106, plink+pw).

**Decided stack (user-confirmed):** Hotword = ONNX (from user's own training server, runs CPU, **pinned/always-resident**); STT = whisper.cpp (GGML/CUDA); LLM = llama.cpp (small Qwen Q4); **TTS = F5-TTS CLONED voice** (user wants voice clone) + lip video from the avatar API. **F5 caveat: no light C++ runtime → run as a managed GPU worker (Python) that ModelManager loads/unloads; GPU arbiter must keep F5 and LLM from co-residing on the 8GB Orin (drain-then-switch).** Expect a few sec TTS latency.

**Architecture:** PyQt UI (screen/video/pairing) ⟷ C++ orchestrator (mic/models/state machine) over a **Unix-socket JSON-lines IPC** at `$XDG_RUNTIME_DIR/avatar-voice.sock`. State machine: IDLE→LISTENING(STT)→THINKING(LLM)→SPEAKING(TTS)→IDLE. UI owns the API/pairing & passes avatar data (idle/lip video, voice, hotword-model URL) to the orchestrator.

**BUILT + VERIFIED (skeleton, step 1):** repo `~/avatar-voice/` (laptop mirror `C:\Users\HP\avatar-voice\`): header-only `include/{json_min,model_manager,ipc_server,pipeline}.h` + `src/main.cpp` + `Makefile` (build: `cd ~/avatar-voice && make` → `./avatar-voice`, deps: just pthread). **ModelManager** = the core: per-model {pinned,usesGpu,vramMB,idleTtlMs,doLoad/doUnload}; `acquire()` evicts Ready non-pinned GPU models oldest-first (drain-then-switch) to fit VRAM_BUDGET_MB=4500; idle reaper thread unloads past-TTL. Stages are TIMED STUBS now (real engines plug into doLoad/doUnload). Verified wake→ stt load → llm load → (stt+llm UNLOADED) → tts load → idle, with model events broadcast to UI.

**UI side:** `backend/orchestrator.py` (Orchestrator QObject, QLocalSocket to the sock, exposes `Voice.state/transcript/reply`, `Voice.wake()`); registered as `Voice` in main.py; `AvatarView.qml` reflects listening/thinking/speaking (orb colour + status + transcript/reply) with a tap-to-wake dev trigger (MouseArea→Voice.wake) until real hotword.

**Run (both, weston must be up):** `systemd-run --user --unit=avatarvoice --setenv=XDG_RUNTIME_DIR=/run/user/1000 /home/jetson/avatar-voice/avatar-voice` ; UI as in [[jetson-qt-kiosk]]. (GOTCHA: plink multi-cmd with bg returns 128 — processes still start; verify with pgrep.)

**2026-06-13 PIVOT: full pipeline = VAD→STT→LLM→TTS, NO hotword (VAD-triggered), strictly C++ (user). TTS = F5 cloned voice, voice ref from avatar API `avatar_voice_link`.**

**KEY ARCH DECISION — subprocess workers:** whisper.cpp & llama.cpp each bundle their own ggml → linking both in one binary clashes. So STT & LLM run as SEPARATE C++ worker executables the orchestrator spawns/kills. "Unload" = kill process = guaranteed full VRAM release (great for offload). Orchestrator links ONLY alsa+pthread (stays tiny). ModelManager.doLoad = spawn worker + wait "READY"; doUnload = kill.

**Code written (`~/avatar-voice/`, mirror `C:\Users\HP\avatar-voice\`):** new headers `audio_alsa.h` (ALSA 16k mono cap/playback), `vad.h` (energy VAD + hangover, onStart/onUtterance cbs; swap Silero-ONNX later), `whisper_stt.h` (whisper.cpp wrap), `llama_llm.h` (llama.cpp wrap, current API: llama_model_load_from_file/llama_init_from_model/llama_model_get_vocab/llama_sampler_chain/llama_memory_clear; ChatML Qwen prompt), `subprocess.h` (fork/exec pipes). New `src/stt_worker.cpp` (proto: stdin `STT <path.f32>`→`TXT ...`, loads whisper once), `src/llm_worker.cpp` (`GEN <text>`→`RPL ...`). Rewrote `pipeline.h` (VAD utterance→worker STT→worker LLM→TTS placeholder, worker thread+queue) + `src/main.cpp` (ALSA capture thread→VAD→pipeline; models stt/llm/tts in ModelManager; mic="default") + `Makefile` (3 targets: avatar-voice[-lasound -lpthread], stt-worker[whisper], llm-worker[llama] with rpaths).

**Engines building (BG, PID was 35625, log /tmp/engines_build.log):** `~/avatar-voice/build_engines.sh` clones+builds whisper.cpp & llama.cpp with `-DGGML_CUDA=ON -DCMAKE_CUDA_ARCHITECTURES=87 -DBUILD_SHARED_LIBS=ON`; downloads ggml-base.en.bin + Qwen2.5-0.5B-Instruct-Q4_K_M.gguf to `~/voice-engines/models/`. Libs land in `whisper.cpp/build/{src,ggml/src}` and `llama.cpp/build/{bin,src,ggml/src}`. SLOW on 4 cores @7W. **After build: `cd ~/avatar-voice && make`, fix any llama API drift, run, test VAD→STT→LLM with real mic.**

**2026-06-13 ENGINES BUILT + STT/LLM VERIFIED ON GPU.** whisper.cpp + llama.cpp built with CUDA (sm_87) under MAXN/6-core. **GOTCHA: `cmake --build` of llama.cpp FAILS linking the `bin/llama` example tool (ld error) — but ALL libs build fine first (`build/bin/libllama.so` + `libggml-cuda.so`; whisper `build/src/libwhisper.so` + `build/ggml/src/libggml*.so`). The tool-link failure is harmless; ignore it (or add -DLLAMA_BUILD_TOOLS=OFF next time).** Models: `~/voice-engines/whisper.cpp/models/ggml-base.en.bin` (142M) + `~/voice-engines/models/qwen2.5-0.5b-instruct-q4_k_m.gguf` (469M). All 3 binaries compiled clean (`make` in ~/avatar-voice) — llama API code was correct, no drift. **VERIFIED: stt-worker transcribed jfk.wav correctly on GPU (use_gpu=1, flash_attn=1); llm-worker replied correctly (CUDA0 Orin, all layers offloaded).** Test recipe: `python3 wav2f32.py jfk.wav /tmp/jfk.f32; printf 'STT /tmp/jfk.f32\nQUIT\n' | ./stt-worker <model>` ; `printf 'GEN hi\nQUIT\n' | ./llm-worker <gguf>`.

**2026-06-13 EOD STATE (resume here tomorrow):** Full orchestrator LIVE — `avatar-voice` runs as `systemd-run --user --unit=avatarvoice` (setenv XDG_RUNTIME_DIR=/run/user/1000, HOME=/home/jetson, AVATAR_MIC=plughw:0,0), holds the USB mic (card0), VAD→STT(worker)→LLM(worker)→TTS-placeholder, broadcasts state over `/run/user/1000/avatar-voice.sock`. UI side panel shows live VOICE state + transcript/reply (untested with real speech — VAD thresholds are first guesses, may need tuning). **Test creds HARDCODED in `backend/device.py`** (TEST_DEVICE_ID=`JETSON_DEV_F83DC61F9F27`, TEST_TOKEN=`sync_f9f54c401bca96d6f8d8d63114f93d0d`, TEST_USER=`user_d927e149`) → skips QR, goes straight to paired, `/sync/avatars` returns 3 avatars (speaker/arijit/APITest-FullGen) with idle+lip mp4 URLs + voice clone wav (s3 avatar-studio-bucket). device.py now exposes `idleVideo/lipVideo/voiceUrl`. **Telemetry shrunk** (SidePanel width 160, fonts 10-12). Main.qml has cross-fade Loader transitions.

**TOMORROW (in order):** 1) finish QtMultimedia install (was installing qml-module-qtmultimedia + libqt5multimedia5-plugins + gstreamer1.0-libav/plugins-good/bad at shutdown) → rewrite `AvatarView.qml` to play `Device.idleVideo` (stream S3 url, loop, muted, ~700x700 centered fits inside ring; swap to lipVideo when Voice.state==speaking) so REAL AVATARS show on screen. 2) Tune VAD on real speech. 3) **F5-TTS strict-C++** = onnxruntime-gpu aarch64 + F5→ONNX export (DakeQQ/F5-TTS-ONNX), voice ref = avatar_voice_link → `tts` worker. 4) wire device→orchestrator set_avatar (pass voiceUrl).

**2026-06-14 VOICE LIVE + tuned + ting + F5-TTS WORKING.** Orchestrator runs via systemd-run (env AVATAR_MIC=plughw:0,0, AVATAR_MIC_GAIN=4). **Tuning (main.cpp+pipeline.h):** 4x mic gain in capture loop; VAD startRms=0.030/endRms=0.018/hangoverMs=700/minSpeechMs=350; **whisper hallucination filter** `Pipeline::isJunk()` drops `(music)`/`[BLANK]`/short junk before LLM. **Ting on VAD detect:** `~/avatar-voice/ting.wav` (gen_ting.py) via `aplay -q -D plughw:0,0` in main.cpp onStart (AVATAR_TING=0 disables). USB card0 = mic+speaker (full-duplex OK). VERIFIED live: clean transcripts, coherent replies, chime heard. Whisper Turbo downloaded (`ggml-large-v3-turbo-q5_0.bin` 574MB) but NOT wired (stt-worker still base.en).

**F5-TTS CLONED VOICE WORKS via ONNX (no PyTorch):** `~/avatar-voice/f5_worker.py`. Models `~/avatar-voice/f5-onnx/{F5_Preprocess,F5_Transformer,F5_Decode}.onnx` (17/634/29MB from HF `huggingfacess/F5-TTS-ONNX`) + vocab.txt. Runtime=**onnxruntime-gpu 1.24.0 (CUDA EP works)**, installed `--index-url https://pypi.jetson-ai-lab.io/jp6/cu126` (**.io NOT .dev — .dev DNS fails**). Deps: jieba pypinyin pydub soundfile numpy (pip --user, PATH+=~/.local/bin). Ref=speaker `~/avatar-voice/ref/reference.wav`(10s) + ref_text="Kolkata, the capital city of West Bengal...". **GOTCHA: this export's interface ≠ DakeQQ ref script** — Preprocess in: audio[1,1,N]=**fp16**(int16/32768→fp16), text_ids[1,N]=int32, max_duration[]=scalar int64; 7 outs (noise,rope_cos,rope_sin,cat_mel_text,cat_mel_text_drop,qk_rotated_empty,ref_signal_len); Transformer in: those 6 + time_step[]=scalar int32 → denoised (NFE loop: denoised→noise, ts=0..NFE-1); Decode: denoised+ref_signal_len→audio fp16 @24kHz. f5_worker.py has the CORRECT port. **SPEED: NFE=32 → 22.6s for 5.8s audio (RTF~3.9, SLOW) — reduce NFE (16/8).** VERIFIED: cloned-voice wav generated + played.

**2026-06-14 (later) — F5 INTEGRATED + FULL PIPELINE VERIFIED END-TO-END + PERSISTENT SERVICE.** Box now on **Bikram Wi-Fi @ 192.168.0.106** (plink+pw jetson/jetson, hostkey SHA256:KxtBVmjRa/nYfzKEBDcjvjt7Puh5uNe4p12bvCWMMl8; plink.exe at `C:\Users\HP\.local\bin\`). **F5 wired into orchestrator TTS stage** (no more placeholder): main.cpp ttsM doLoad spawns `/usr/bin/python3 f5_worker.py --worker` (waits READY), vramMB=2600, idleTtlMs=120000; **VRAM_BUDGET_MB 4500→6000**; pipeline.h speaking phase = `f5_.writeLine("GEN "+reply); f5_.readLine()` (blocks to "RPL done"). f5_worker.py: NFE=16 (F5_NFE env), CUDA gpu_mem_limit 2600MB. **VERIFIED LIVE: played jfk.wav into card0 → transcript "and so over the middle of America" (STT base.en) → LLM reply → F5 wrote fresh /tmp/f5_out.wav (439KB, CUDAExecutionProvider, NFE16) + played in cloned voice.** Standalone F5 NFE16 = 4.8s audio in 14.0s. STT still base.en (turbo NOT wired). LLM=qwen2.5-0.5b-instruct-q4.

**PERSISTENT SERVICE (KEY FIX):** orchestrator now `~/.config/systemd/user/avatarvoice.service` (Type=simple, Restart=always, Env XDG_RUNTIME_DIR=/run/user/1000 HOME AVATAR_MIC=plughw:0,0 AVATAR_MIC_GAIN=4 F5_NFE=16, ExecStartPre rm stale sock, StandardOutput=append:/tmp/av.log). **GOTCHA SOLVED: service kept dying with clean "avatar-voice stopped" right after start — the user systemd manager was torn down on SSH disconnect (no lingering) → SIGTERM. FIX = `sudo loginctl enable-linger jetson` (Linger=yes). Now survives disconnect, PID stable, socket /run/user/1000/avatar-voice.sock up.** Manage via `systemctl --user {restart,status} avatarvoice` (export XDG_RUNTIME_DIR + DBUS_SESSION_BUS_ADDRESS=unix:path=/run/user/1000/bus). User-journal not persistent → read /tmp/av.log instead.

**HOTWORD per-avatar — USAGE BUILT (dormant), TRAINING BLOCKED server-side.** New always-on gateway `https://65-2-7-128.sslip.io/api` (key in `D:\music\API_DOCS_WITH_KEY.md`; X-API-Key header; auto-boots/stops GPU `i-089730e889eb1fb3d`; uses **openWakeWord** 3-model ONNX). 3 avatars = speaker/arijit/APITest-FullGen (from /sync/avatars). Submitted "hey speaker/arijit/apitest" (quick: n_samples 2000 steps 5000) → **ALL FAIL @15% generate_piper: `ModuleNotFoundError: No module named 'dp'` (DeepPhonemizer missing in box's /opt/hotword/venvs/train)** — server-side, NOT client-fixable; older "hey kiki" jobs are done. Once box fixed: re-/train, download 3 .onnx → `~/avatar-voice/hotwords/`, no code change. **Usage code DONE + in binary but dormant:** `hotword_worker.py` (openWakeWord, loads ALL wake words, emits "WAKE <key> <score>", CPU-only via CUDA_VISIBLE_DEVICES=""); pipeline.h gating (setGated/onWake/armed window 10s + 8s followup + watchdog relock); main.cpp tees mic to hwProc, reader thread filters by active avatar (set via set_avatar IPC = avatar name = onnx stem), always-on-VAD fallback when `hotwords/` empty. **Hotword runs from ISOLATED `~/avatar-voice/hw-venv`** (HW_PY in main.cpp). **CURRENTLY DORMANT: `hotwords/*.onnx` moved to `hotwords_disabled/` → orchestrator runs always-on VAD (user asked to focus on STT+F5 only, drop hotword for now).**

**GOTCHA — onnxruntime clobber:** `pip install --user openwakeword` pulled CPU onnxruntime 1.23.2 + numpy 2.2.6 into --user site → **broke F5's onnxruntime-gpu (CUDA EP vanished)**. FIX: `pip uninstall openwakeword tflite-runtime onnxruntime onnxruntime-gpu; pip install --user 'numpy<2'; pip install --user onnxruntime-gpu==1.24.0 --index-url https://pypi.jetson-ai-lab.io/jp6/cu126`. Restored np 1.26.4 + ort-gpu 1.24.0 (CUDA back). **NEVER pip-install openwakeword into the shared --user env again — use hw-venv.** python3.10-venv installed via apt for the venv.

**STILL TODO:** swap STT base.en→large-v3-turbo (downloaded, not wired; +VRAM retune); wire voice→avatar_ui (C++ kiosk show transcript + idle↔lip swap on speaking); un-dormant hotword once training box's `dp` dep fixed.
